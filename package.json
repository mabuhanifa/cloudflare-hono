{
  "name": "cfw-hono-drizzle",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy --minify",
    "cf-typegen": "wrangler types --env-interface CloudflareBindings",
    "db:generate": "drizzle-kit generate",
    "db:up": "drizzle-kit up:sqlite"
  },
  "dependencies": {
    "@libsql/client": "^0.15.12",
    "drizzle-kit": "^0.31.4",
    "drizzle-orm": "^0.44.4",
    "hono": "^4.9.2"
  },
  "devDependencies": {
    "wrangler": "^4.4.0"
  }
}
